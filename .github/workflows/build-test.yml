name: Build Test Pipeline

on:
  pull_request:
    branches: [main]

jobs:
  build-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run build test
        run: |
          echo "Build test failed"
          exit 1
